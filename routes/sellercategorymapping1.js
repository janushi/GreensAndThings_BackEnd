var express = require('express');
var router = express.Router();
var sellercategorymapping1=require('../models/sellercategorymapping1_model');

router.get('/:id?',function(req,res,next){
 
if(req.params.id){
 
sellercategorymapping1.getsellercategorymapping1ById(req.params.id,function(err,rows){
 
if(err)
  {
  res.json(err);
  }
  else{
  res.json(rows);
  }
  });
 }
 else{
 
sellercategorymapping1.getAllsellercategorymapping1(function(err,rows){
 
if(err)
  {
  res.json(err);
  }
  else
  {
  res.json(rows);
  }
 
 });
 }
 });

 router.post('/',function(req,res,next){

sellercategorymapping1.addsellercategorymapping1(req.body,function(err,count){
  if(err)
  {
  res.json(err);
  }
  else
  {
  res.json(req.body);//or return count for 1 &amp;amp;amp; 0
  }
  });
 });

 router.delete('/:id',function(req,res,next){
 
sellercategorymapping1.deletesellercategorymapping1(req.params.id,function(err,count){
 
if(err)
  {
  res.json(err);
  }
  else
  {
  res.json(count);
  }
 
});
 });
router.put('/:id',function(req,res,next){
 
sellercategorymapping1.updatesellercategorymapping1(req.params.id,req.body,function(err,rows){
 
if(err)
  {
  res.json(err);
  }
  else
  {
  res.json(rows);
  }
  });
 });
 module.exports = router;